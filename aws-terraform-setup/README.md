Provision AWS infrastructure using Terraform to host a public web server (Nginx) in a secure and modular environment.


```bash
1. Terraform Commands

terraform init
terraform fmt
terraform validate
terraform plan
terraform apply -auto-approve

```bash

